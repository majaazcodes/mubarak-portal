# Public API URL — must point at the backend the phone can reach.
# In dev, use a cloudflared tunnel URL since LAN is firewalled:
#   cloudflared tunnel --url http://localhost:4000
# Then paste the public URL here with the /api/v1 suffix:
EXPO_PUBLIC_API_URL=https://your-tunnel.trycloudflare.com/api/v1
